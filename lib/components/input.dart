import 'package:flutter/material.dart';
import 'package:shared_preferences/shared_preferences.dart';

class InputWidet extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    
    return TextField(
      
    );
  }
}